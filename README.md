# esp8266_antiparallel_leds
MQTT based anti parallel LED chain driver using an ESP8266 and an H bridge.

Configuration can be achieved by renaming the `config_template.h` to `config.h` and adjusting it to your environment.

The device uses MQTT discovery payloads to seamelessly integrate with Home Assistant.
